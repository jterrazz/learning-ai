inbound:
    env: test
    http:
        host: localhost
        port: 3000
    logger:
        level: debug
        prettyPrint: true

outbound:
    intelligence:
        providers:
            openrouter:
                apiKey: test-openrouter-key
